# 제작 실록

커밋 24개를 시간순으로 정리한 기록. 작품 설명은 [`README.md`](README.md),
기술적 교훈은 [`tasks/lessons.md`](tasks/lessons.md)에 있다. 이 문서는
**무엇을 어떤 순서로 했고 왜 그 순서였는가**만 다룬다.

전체 작업은 2026-08-02 22:20에 시작해 08-08 18:39에 멈췄다.

---

## 1막 — 렌더 (08-02 22:20, 1커밋)

| 커밋 | 내용 |
|---|---|
| `10ee407` | 심청전 8막 절차적 Three.js 렌더 |

8막 7분 26초, TypeScript strict 약 11,400줄, 에셋 0개가 **한 커밋으로** 들어온다.
이 시점에는 하네스도 감사 절차도 없다. 만들어 놓고 나서야 문제가 드러난다 —
**LLM은 자기가 만든 렌더를 볼 수 없다.**

## 2막 — 벡터화 감사 하네스 (08-02 22:31 ~ 08-03 00:49, 15커밋)

작품보다 **눈을 만드는 데 더 많은 커밋이 들어갔다.** 15 대 1이다.

| 커밋 | 내용 |
|---|---|
| `62fb768` `b86a17b` | 배열형 렌더 하네스 설계 스펙과 구현 계획 |
| `980293e` | SoA 매니페스트와 무결성 검사기 |
| `f941d84` | `audit:test`가 테스트를 하나도 돌리지 않던 문제 |
| `62541c9` `aed4caf` | 일괄 프레임 캡처, 실패 시 정리와 불필요한 재빌드 대기 제거 |
| `36446c6` | 레인 워커와 스칼라 테일 역할 정의 |
| `ba79547` | 감사 절차 스킬과 하네스 상태 문서 |
| `7e82454` | 감사 프레임의 콘솔 게이트와 UI 노이즈 정리 |
| **`15ca50a`** | **전환 중 `goTo`가 전환을 무한히 리셋하던 문제** |
| `aefa91a` | 프레임이 거짓말한 사건을 교훈과 절차에 반영 |
| `1c59da9` | 벡터 라운드 2 — 정상 프레임으로 재판정 |
| `bbb58b5` `e3ab3d8` `4a12368` | `captureAt` 고유성 검사, 레인별 실패 격리, `settle()`이 스톨에서 실제로 실패하게, 파일 소유권 주장 정정 |

### 이 프로젝트의 분기점 — `f941d84`와 `15ca50a`

두 커밋이 같은 종류의 사고다. **실패가 아무 신호도 내지 않는 상태.**

`f941d84`는 감사 테스트가 **0개를 조용히 실행하고 통과처럼 보이던** 것을 고쳤다.
`15ca50a`는 더 나빴다 — 감사 하네스가 프레임 33장을 뽑았는데 세 막에서 서로 다른
시각의 프레임이 **바이트 단위로 동일**했다. 예외도 콘솔 에러도 없었다.

원인은 `Director.goTo`가 전환(페이드) 중에 호출되면 `transition`을 다시 1.1초로
리셋해, 1.1초보다 빠르게 점프를 던지는 호출자는 seek 분기에 영영 진입하지 못하는
것이었다. 막은 `enter()`가 잡은 시각에 얼어붙는다.

**레인 에이전트 8개가 그 프레임을 믿고 멀쩡한 씬을 "고칠" 뻔했다.** 크래시는 즉시
드러나지만 거짓 프레임은 잘못된 수정을 낳는다.

그 자리에서 나온 대응이 이 저장소의 핵심 장치가 됐다 — 상태를 밖에서 볼 수 있게
(`simcheong.state()`), 고정 `sleep` 대신 상태 폴링, 매 캡처마다 시크 착지 검증,
그리고 **서로 다른 시각의 프레임은 md5가 같으면 실패**. 자세한 전말은
[`tasks/lessons.md`](tasks/lessons.md) 10·11번.

한 레인의 행동이 특히 기록할 만하다. `vow` 레인은 프레임 세 장이 동일하다는 것을
알아채고 **수정 0건, `verdict: unknown`으로 반환**했다. 낡은 프레임을 근거로 안개
밀도를 바꾼 다른 레인과 대조된다. "확인할 수 없으면 고치지 않는다"가 실제로
잘못된 수정을 막은 사례다.

> **여기까지가 이 저장소에 먼저 공개된 스냅샷**(`3290922`)이다. 아래 3·4막은
> 이번에 추가된다.

## 3막 — 오디오 (08-03 08:07 ~ 08:26, 3커밋)

| 커밋 | 내용 |
|---|---|
| `3b9c281` | 판소리 세 목소리 나레이션 재생 |
| `09afd71` | 키를 `FISH_API_KEY`에서 읽고, 상속 함정을 문서화 |
| `c3a835b` | 웹 UI 붙여넣기용 녹음 대본과 수동 매니페스트 |

자막이 이미 판소리의 register를 따르고 있었다 — 말로 하는 **아니리**, 소리로 하는
**창**, 고수의 **추임새**. 오디오는 그 구분을 세 목소리로 옮긴 것이다.

`c3a835b`이 방향 전환이다. TTS API 자동화 대신 **웹 UI에 붙여넣을 대본을 생성**하는
쪽으로 갔다. 서드파티 UI를 자동화하지 않고, 수동 왕복을 결함이 아니라 설계된
인터페이스로 삼는다. 대본은 `narrator.ts`의 자막 데이터에서 파생되므로 손으로
맞추는 곳이 없다.

## 4막 — 게시 자료 (08-08, 5커밋)

닷새 뒤, 영상을 내보내기 위한 자료가 한꺼번에 붙는다.

| 커밋 | 산출물 |
|---|---|
| `162bffe` | `seedance-prompts.md` — 20초 클립 프롬프트 25개를 사설과 대조해 맞춤 + `seedance-edit-map.md` |
| `5feca2a` | `audio-script-bare.md` 붙여넣기 전용 출력 + `suno-prompts.md` 배경음악 8곡 |
| `7e1b99f` | `ending-credits.md` — 판이 닫히는 자리 |
| `d83fb7b` | `youtube.md` — 제목·설명·태그·타임스탬프 |
| `2766699` | `thumbnail.md` — GPT Image 2 썸네일 지침 |

3막의 규약이 그대로 확장됐다. Seedance·Suno·GPT Image 2 모두 API 없이 웹 UI만
있으므로, **프롬프트 문서가 작업지시서 겸 배치 계약**이 된다. 문서는 전부
생성물이라 원본 데이터를 고치고 다시 뽑는다.

---

## 지금 상태

| | |
|---|---|
| 렌더 | ✅ 8막 7분 26초, 에셋 0개. [실행](https://reasonofmoon.github.io/shim-cheong/) |
| 감사 하네스 | ✅ 배열형 8레인 + 스칼라 테일, 벡터 2라운드 상한 |
| 나레이션 대본 | ✅ 생성기 완비 (`audio-script.md`, `audio-script-bare.md`) |
| 나레이션 음원 | ⬜ 웹 UI 수동 수급 |
| 배경음악 | ⬜ Suno 프롬프트 8곡 준비, 곡 미수급 |
| Seedance 클립 | ⬜ 프롬프트 25개 준비, 클립 미수급 |
| 게시 | ⬜ 유튜브 자료·썸네일 지침 준비, 업로드 전 |

즉 **코드로 만들 수 있는 것은 끝났고, 외부 도구에서 받아올 것이 남았다.**

## 알아둘 것

- 이 저장소는 원래 `video-auto`라는 큰 저장소의 한 브랜치에서 자랐다. 그 브랜치에는
  무관한 Electron 앱도 함께 들어 있어, 공개본에는 심청전에 속한 것만 옮겼다.
  따라서 커밋 해시는 원 브랜치의 것이고 이 저장소의 이력과 1:1로 대응하지 않는다.
- `HARNESS_STATE.md`의 "드리프트 위험" 절은 **아직 유효한 미해결 항목**이다.
  특히 레인 소유권은 구조적 보장이 아니라 계약이며, 리듀스의 `git diff` 감사는
  레인 간 위반을 잡지 못한다.
